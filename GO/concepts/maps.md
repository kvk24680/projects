**Maps** are Go's built-in associative data type. They are sometimes also called **hashes** or **dicts** in other languages.

To create an empty map, use the builtin make: 
```bash
make(map[key-type]val-type).
```
